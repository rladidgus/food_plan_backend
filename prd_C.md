# [PRD] Node C: 메뉴 영양 정보 추출 에이전트 (Fetch Nutrition Facts)

## 1. 개요 (Introduction)
본 모듈은 사용자가 선택한 메뉴 아이템 리스트(`menu_item_ids`)를 기반으로, 해당 음식들의 상세 영양 성분 및 칼로리 데이터를 조회하고 매핑하는 역할을 수행한다. 이 데이터는 시스템 내에서 사용자의 건강 상태를 분석하고 맞춤형 피드백을 제공하는 데 핵심적인 기초 자료가 된다.

---

## 2. 목적 및 배경 (Goals & Background)
* **데이터 정량화**: 텍스트 형태의 메뉴명을 수치화된 영양 데이터(칼로리, 탄단지 등)로 변환한다.
* **확장성 확보**: 단순 DB 조회를 넘어, 향후 LLM 에이전트를 통해 모호한 메뉴에 대한 영양 성분 추론이 가능하도록 구조를 설계한다.
* **의사결정 지원**: 분석된 영양 정보를 바탕으로 사용자에게 식단 추천 또는 경고 메시지를 보낼 수 있는 근거를 마련한다.

---

## 3. 핵심 기능 요구사항 (Functional Requirements)

### 3.1 메뉴-영양 데이터 매핑
- **[REQ-01]** 입력된 각 `menu_item_id`에 해당하는 영양 성분 프로필을 조회해야 한다.
- **[REQ-02]** 조회 성공 시, 해당 영양 정보의 고유 식별자인 `nutrition_id`를 리스트 형태로 반환한다.

### 3.2 데이터 처리 및 예외 관리
- **[REQ-03]** 입력 리스트가 비어있을 경우, 시스템 오류를 방지하기 위해 빈 리스트를 반환한다.
- **[REQ-04]** 매칭되는 영양 정보가 없는 메뉴의 경우, 시스템 로그를 남기고 '미분류/기본값' ID를 할당한다.

### 3.3 에이전트 인터페이스 (Future-proof)
- **[REQ-05]** 단순 조회 실패 시 외부 API(식약처 등) 또는 LLM을 호출하여 영양 정보를 동적으로 생성할 수 있는 인터페이스를 유지한다.

---

## 4. 기술 명세 (Technical Specifications)

### 4.1 데이터 모델 (Data Flow)
| 단계 | 항목 | 데이터 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Input** | `menu_item_ids` | `List[int]` | 분석 대상 메뉴 ID 리스트 |
| **Process** | `lookup_nutrition` | `Function` | DB 또는 에이전트를 통한 데이터 매칭 |
| **Output** | `nutrition_ids` | `List[int]` | 매핑된 영양 정보 식별자 리스트 |

### 4.2 성능 목표 (Non-functional)
- **응답 속도**: 단일 호출 시 500ms 이내 처리 (DB 조회 기준).
- **정확도**: 기존 확정형 메뉴에 대한 매칭 정확도 100% 유지.

---

## 5. 제약 사항 (Constraints)
- 모든 영양소 데이터는 표준 단위(kcal, g, mg)로 정규화되어야 한다.
- 개인정보 보호를 위해 영양 정보 조회 단계에서는 사용자의 식별 정보를 직접 포함하지 않는다.

---

## 6. 성공 지표 (Success Metrics)
- **Data Coverage**: 전체 메뉴 데이터 중 영양 정보가 결합된 메뉴의 비율 98% 달성.
- **Processing Rate**: 식단 분석 프로세스 중 Node C에서 발생하는 타임아웃 오류 0.1% 미만 유지.