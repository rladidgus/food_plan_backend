# PRD: 인바디 맞춤형 식단 수집 및 실시간 추천 시스템

## 1. 개요

### 1.1 목적
사용자의 위치(집/회사) 기반 음식점 데이터를 선제적으로 수집하여 고품질 DB를 구축하고, 사용자의 신체 상태(인바디) 및 목표(`goal_type`) 변경 시 즉각적으로 맞춤형 식단을 추천하는 시스템을 구현한다.

### 1.2 시스템 분리 전략 (Decoupling)

사용자가 마이페이지에서 목표를 바꿀 때마다 무거운 수집 과정을 반복하지 않도록 **수집(Collector)**과 **추천(Recommender)** 단계를 논리적으로 완전히 분리한다.

| 구분 | 모듈 | 실행 트리거 | 주요 특징 |
|:---|:---|:---|:---|
| **Phase 1. 수집 Agent** | **Data Collector** (Node A, B, C) | 주소 등록/변경 시, 주기적 갱신 | Heavy & Slow (외부 API, 스크래핑, LLM) |
| **Phase 2. 추천 Service** | **Real-time Recommender** (Node D) | 목표 변경 시, 식단 조회 시 | Light & Fast (DB 쿼리, 산술 연산) |

---

## 2. 시스템 아키텍처



### 2.1 데이터 흐름
1.  **Collector Agent**: Kakao API → 웹 스크래핑 → LLM 영양 추론 → **데이터 무결성 검증** → DB 저장.
2.  **Verified DB**: 모든 정보(가게-메뉴-가격-영양)가 갖춰진 '추천 가능 메뉴 풀' 유지.
3.  **Recommender Service**: 유저의 현재 `goal_type`에 맞춰 DB에서 즉시 필터링 후 끼니별 Top 3 반환.

---

## 3. 노드별 상세 스펙

### 3.1 [Phase 1] 데이터 수집 Agent (The Heavy Lifter)

**목적**: 추천에 사용할 수 있는 '완전한 데이터'를 확보하여 DB에 적재.

* **Node A: `A_fetch_restaurants`**
    - **동작**: Kakao Local API를 통해 반경 500m 내 음식점 **50곳**을 거리순으로 수집.
    - **필수 데이터**: `source_place_id`, `name`, `distance_m`, `lat`, `lng`.
* **Node B: `B_fetch_menus`**
    - **동작**: 수집된 가게의 `place_url`을 통해 메뉴명, 가격, 설명 추출.
    - **검증**: 메뉴명 또는 가격 정보가 없는 경우 해당 음식점은 다음 단계로 넘기지 않음.
* **Node C: `C_fetch_nutrition`**
    - **동작**: 웹 검색 및 LLM을 활용해 칼로리 및 탄/단/지 성분 추론.
    - **Strict Skip Rule (데이터 무결성)**:
        - [메뉴명, 가게 정보, 가격, 영양성분(칼로리/탄/단/지)] 중 하나라도 누락되면 DB에 저장하지 않고 버림.
        - LLM 추론 결과의 신뢰도(`confidence`)가 낮을 경우에도 Skip.

### 3.2 [Phase 2] 실시간 추천 Service (The Speed Demon)

**목적**: 사용자의 상태 변경(목표, 인바디)에 즉각 응답하여 개인화된 식단 제공.

* **Node D: `D_generate_personalized_menu`**
    - **실행 트리거**: 사용자가 마이페이지에서 목표(`goal_type`)를 변경하거나 식단 탭에 진입할 때 즉시 호출.
    - **필터링 로직**:
        1. **인바디 연동**: 기초대사량(BMR)과 활동량을 바탕으로 $TDEE$(총 에너지 소비량) 계산.
        2. **목표 적용**: `goal_type`(다이어트, 벌크업 등)에 맞춰 한 끼 권장 칼로리 및 탄단지 가이드 산출.
        3. **DB 쿼리**: `verified_menu_pool`에서 가이드 오차범위(±15%) 내의 메뉴를 실시간 검색.
    - **최종 출력**: 아침, 점심, 저녁별 **가게-메뉴(1개)-가격-영양정보** 세트를 각 3순위까지 표시.

---

## 4. DB 스키마 (추천 가속화 구조)

추천 서비스가 수백 개의 메뉴를 순식간에 훑을 수 있도록 `Verified` 상태를 관리하는 것이 핵심이다.

### 4.1 `verified_menu_pool` (Table/View)
추천 서비스(Node D)가 직접 참조하는 최적화된 데이터 세트.

| 컬럼명 | 설명 | 비고 |
|:---|:---|:---|
| `restaurant_id` | 음식점 고유 ID | FK |
| `menu_name` | 메뉴 명칭 | NOT NULL |
| `price` | 가격 (원) | NOT NULL |
| `calories_kcal` | 칼로리 | NOT NULL |
| `carbs_g`, `protein_g`, `fat_g` | 탄/단/지 함량 | NOT NULL |
| `distance_m` | 기준 위치로부터의 거리 | 실시간 계산 또는 스냅샷 |

---

## 5. 사용자 경험(UX) 및 예외 처리

### 5.1 목표 변경 시나리오
1.  **사용자**: 마이페이지에서 '체지방 감량' → '근육량 증가(벌크업)'로 목표 수정.
2.  **시스템**: 유저 테이블의 `goal_type`을 업데이트하고, 즉시 **Node D** 함수 실행.
3.  **결과**: 사용자는 대기 시간(수집 시간) 없이 1초 내에 "고단백/고칼로리" 위주의 새로운 식단 3안을 확인.

### 5.2 예외 처리 (Fallback)
- **추천 후보 부족**: 500m 내에서 데이터 무결성을 통과한 메뉴가 끼니별 3개 미만일 경우, 수집 Agent(Phase 1)를 깨워 검색 반경을 1km로 확장하고 데이터를 추가 확보함.
- **인바디 미입력**: 사용자의 인바디 정보가 없을 경우, 표준 신장/체중 기반 권장치를 기본값으로 활용.

---

## 6. 기술 스택

| 구분 | 기술 |
|------|------|
| **Agent 관리** | LangGraph (Phase 1 수집 파이프라인 제어) |
| **백엔드/API** | FastAPI (Phase 2 실시간 추천 엔진 서빙) |
| **데이터베이스** | PostgreSQL (PostGIS 활용하여 거리 기반 쿼리 최적화) |
| **LLM** | GPT-4o (영양 정보 추론용) |