# PRD: 인바디 맞춤형 식단 데이터 수집 및 추천 Agent

## 1. 개요

### 1.1 목적
사용자의 위치(집/회사)를 기반으로 반경 내 음식점 데이터를 수집하되, **데이터의 완전성(메뉴·가격·영양정보)**을 엄격히 검증하여 사용자의 **인바디 상태 및 목표(goal_type)**에 최적화된 식단 후보(끼니별 3안)를 도출한다.

### 1.2 핵심 전략
- **데이터 무결성 게이트**: 메뉴명, 가게 정보, 영양성분, 가격 중 하나라도 누락된 데이터는 추천 대상에서 즉시 제외(Skip)하여 신뢰도를 높임.
- **인바디 연동 필터링**: 사용자의 인바디 수치(기초대사량, 활동량 등)로 계산된 권장 섭취량과 목표(다이어트/유지/벌크업)를 기준으로 수집된 데이터를 필터링함.
- **최종 출력**: 아침, 점심, 저녁 각각에 대해 최적의 '가게-메뉴-가격-영양정보' 세트를 3가지씩 제안.

---

## 2. 시스템 아키텍처 (LangGraph 파이프라인)



### 2.1 처리 흐름
1. **Node A (Fetch Restaurants)**: Kakao API를 통해 거리순으로 50개 음식점 수집.
2. **Node B (Fetch Menus & Price)**: 각 음식점의 메뉴명, 가격, 설명 추출.
3. **Node C (Fetch Nutrition & Validation)**: 영양정보 수집 및 **데이터 완전성 검증(누락 시 Skip)**.
4. **Node D (Goal-based Filtering)**: 인바디 기반 목표 칼로리/탄단지 비율에 맞춰 필터링 및 끼니별 Top 3 선정.

---

## 3. 노드별 상세 스펙

### 3.1 Node A: `A_fetch_restaurants_distance`
- **목적**: 위치 기준 최단 거리 음식점 확보
- **데이터 소스**: Kakao Local API (`/v2/local/search/keyword.json`)
- **파라미터**: `radius=500`, `sort=distance`, `size=15` (3페이지 반복 호출하여 총 **50개** 확보)
- **출력**: `restaurants[]` (거리순 정렬된 리스트)

### 3.2 Node B & C: `BC_validate_data_integrity`
- **목적**: 추천 가능한 수준의 '완전한 데이터'만 선별
- **처리 로직**:
  1. **메뉴 수집**: 음식점별 대표 메뉴, 가격, 설명 스크래핑.
  2. **영양 수집**: 검색 API 및 LLM 추론을 통해 칼로리, 탄단지 정보 확보.
  3. **데이터 필터링 (Strict Skip Rule)**:
     - 아래 항목 중 하나라도 `None` 또는 `0`인 경우 DB 저장 및 추천 후보에서 제외.
     - **체크리스트**: [가게 기본 정보, 메뉴명, 가격, 영양성분(칼로리/탄/단/지)]
- **결과**: 검증을 통과한 `verified_menu_pool` 생성.

### 3.3 Node D: `D_generate_personalized_menu`
- **목적**: 인바디 기반 맞춤형 식단 구성
- **입력**: 사용자의 인바디 정보(`goal_type`, 기초대사량, 목표 칼로리), `verified_menu_pool`
- **필터링 기준**:
  - **칼로리 매칭**: (일일 권장 칼로리 / 3) $\pm 15\%$ 오차 범위 내 메뉴 선정.
  - **영양 비율**: `goal_type`(예: 고단백 다이어트)에 따른 탄단지 비중 필터링.
- **최종 출력**: 아침, 점심, 저녁별로 각각 **3개씩**의 대안 리스트 생성.

---

## 4. 데이터베이스 및 출력 스펙

### 4.1 필수 데이터 구조 (`menu_items` & `nutrition_facts`)
추천 엔진에서 활용하기 위해 모든 필드는 `NOT NULL`을 지향한다.

| 항목 | 필드명 | 중요도 | 비고 |
|------|------|------|------|
| 가게정보 | `restaurant_name`, `address` | **필수** | Kakao API 기반 |
| 메뉴정보 | `menu_name`, `price` | **필수** | 스크래핑 기반 (0원 이하 제외) |
| 영양정보 | `calories`, `carbs`, `protein`, `fat` | **필수** | 검색/LLM 추론 (누락 시 Skip) |

### 4.2 최종 사용자 표시 포맷
사용자 인터페이스(UI)에 표시될 끼니별 데이터 예시:

> **[점심 식단 추천]**
> * **1순위**: [가게A] 닭가슴살 샐러드 (9,500원) - 450kcal (탄30/단35/지10)
> * **2순위**: [가게B] 연어 포케 (12,000원) - 510kcal (탄40/단30/지15)
> * **3순위**: [가게C] 소고기 미역국 백반 (8,500원) - 480kcal (탄50/단25/지12)

---

## 5. 예외 처리 전략

1. **검증 통과 데이터 부족**: 반경 500m 내에서 데이터가 완전한 음식점이 끼니별 3개 미만일 경우, 검색 반경을 1km로 확장하여 Node A부터 재실행한다.
2. **영양정보 불확실성**: LLM 추론 결과의 `confidence` 점수가 0.6 미만일 경우, 신뢰할 수 없는 데이터로 간주하여 Skip 처리한다.
3. **인바디 데이터 부재**: 사용자 인바디 정보가 없을 경우, 표준 신장/체중 기반 권장량을 기본값으로 적용한다.