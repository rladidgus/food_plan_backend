# 데이터베이스 설정 (기존 설정과 동일)
DB_HOST=localhost
DB_NAME=postgres
DB_USER=postgres
DB_PASS=password
DB_PORT=5432
DB_RESET=0

# API 설정
API_PORT=8000

# Next.js 프론트엔드 URL (CORS 설정용)
FRONTEND_URL=http://localhost:3000
